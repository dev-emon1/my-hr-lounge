import AuthWrapper from "../components/auth-layout-wrapper";

import LoginForm from "../components/login-form";

function LoginPage() {
  return (
    <AuthWrapper>
      <LoginForm />
    </AuthWrapper>
  );
}

export default LoginPage;
